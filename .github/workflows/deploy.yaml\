name: "deploy cf"

on:
    push:
        branches: ["main"]

jobs:
    deploy-cf:
        runs-on: ubuntu-latests
        steps:
            run: echo "do pull"
            run echo "make migrations"
            run: echo "bye bye"